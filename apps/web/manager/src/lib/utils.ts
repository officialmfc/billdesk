/**
 * Re-export utilities from @mfc/utils package
 * This maintains backward compatibility while using shared utilities
 */
export { cn, formatCurrency, formatDate, formatWeight, formatPhone, formatPercentage } from '@mfc/utils';
